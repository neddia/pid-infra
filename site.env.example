# Per-site identity & network
SITE_ID=site-003
HOSTNAME=pid-site-003

# Tailscale join (use preauth key with tags)
TS_AUTHKEY=tskey-xxxxxxxxxxxxxxxxxxxxxxxx
TS_TAGS=tag:edge,tag:prod

# Optional app deployment
# APP_REPO=https://github.com/you/pid-app.git
# APP_TAG=v1.0.0

# Optional Influx overrides
# INFLUX_ORG=pid
# INFLUX_BUCKET=metrics
